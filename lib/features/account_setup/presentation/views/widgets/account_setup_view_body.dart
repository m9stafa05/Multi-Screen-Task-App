import 'package:flutter/material.dart';

class AccountSetupViewBody extends StatelessWidget {
  const AccountSetupViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: const Text("Acc"));
  }
}
